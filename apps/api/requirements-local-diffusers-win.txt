# Minimal runtime for local FastAPI + real (diffusers) image generation on Windows.
# Use with Python 3.12. Install torch separately via the CPU wheel index.

fastapi
uvicorn[standard]
python-dotenv
requests
Pillow
PyJWT
redis

# Diffusion stack
huggingface_hub
safetensors
transformers
accelerate
diffusers

# Optional but commonly needed by pipelines
numpy

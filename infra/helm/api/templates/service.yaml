apiVersion: v1
kind: Service
metadata:
  name: {{ include "makerkit-api.fullname" . }}
  labels:
    app: {{ include "makerkit-api.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "makerkit-api.name" . }}
    release: {{ .Release.Name }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
